name: Hello Actions

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo 'Hello, GitHub Actions'
      - name: Print time
        run: date -u
